# 2 numbers add program using function-- return sum-- Input from the user
def add ():
    num1 = int(input('Enter a first number : '))
    num2 = int(input('Enter a second number : '))
    result = num1 + num2
    return result

tottal_result = add()
print(tottal_result)
